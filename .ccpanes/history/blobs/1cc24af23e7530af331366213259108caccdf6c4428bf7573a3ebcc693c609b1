# FableMap Godot 集成设计（历史参考）

## 文档定位

这份文档记录的是 FableMap 早期把 Godot 作为主要消费层时的一套接入设想。

它**不再代表当前主线实现方向**。

当前项目主线已经收敛为：

> 真实底图 + 地点选择 + 角色遭遇 / 地点事件 + 聊天叙事 + writeback / memory

因此，Godot 不再是当前近期主承载体。本文档只保留为：

- 历史阶段参考
- 下游消费层设想参考
- 结构化世界数据如何被 3D / 场景系统消费的示意说明

---

## 它当时试图解决什么

在早期阶段，仓库已经能生成结构化 `world.json`，但仍缺少一个“可走动、可点击、可感受氛围”的场景消费层。

把 Godot 作为消费端，在当时有几个明确价值：

- 帮助团队把 JSON 世界想象成可探索场景
- 验证结构化字段是否足够驱动下游表现层
- 为后续更重的互动体验提供承载容器

所以，这条路线在历史上是合理的。

---

## 它为什么不再是当前主线

后续工程演进证明：

- 浏览器更适合作为当前入口与快速验证层
- 当前最关键的问题不是“场景是否更沉浸”，而是“地点、角色、事件、聊天与记忆闭环是否成立”
- 把 Godot 拉回主链路，会再次分散当前主线资源
- Godot 无法替代当前协议收束、写回闭环与世界编排工作

因此，Godot 现在更适合作为：

- 历史参考
- 未来可选扩展消费层
- 结构化世界状态的下游使用案例

而不是当前产品与工程的主入口。

---

## 若未来作为扩展消费层，Godot 端可承担什么

如果未来重新引入 Godot，它更适合承担“消费结构化结果”的职责，而不是重新定义世界真相。

### 可承担职责

- 读取稳定的世界 JSON / payload
- 根据地点、区域、角色、事件状态进行场景实例化
- 消费已确定的语义字段、镜头字段与视觉提示字段
- 承载局部沉浸式表现、镜头演出或探索式扩展体验

### 不应承担职责

- 直接解析 OSM 原始数据
- 重新决定幻想语义
- 自行推断 POI 类型
- 重新发明一套与主链路平行的世界状态
- 把 Godot 内部运行结果当成世界唯一真相来源

这些都应由 Python / 服务端 / 协议层先完成。

---

## 推荐消费方式（若未来恢复）

Godot 应只读取稳定字段，而不是依赖易漂移的表现层细节。

### 建议读取的稳定信息

- `world_id`
- `slice_id`
- `region.theme`
- `region.visual_style`
- `pois[*].id`
- `pois[*].fantasy_type`
- `pois[*].position`
- `pois[*].description`
- `poi_states`
- `player_state`
- `events`
- `memory_hooks`

### 设计原则

- Godot 消费的是结构化世界结果，而不是原始地理解析过程
- Godot 端字段依赖应尽量稳定，避免跟随实验字段频繁漂移
- Godot 若失败，不得反向阻塞当前主链路

---

## 可以保留的历史启发

这条旧路线仍然提供了一些有价值的启发：

### 1. 下游消费层应只读取稳定字段

无论是 Web、Godot 还是其他运行时，消费层都不应重新承担语义判定与世界生成职责。

### 2. 世界结构必须可被多种前端消费

如果一个世界协议只能服务单一表现层，说明协议边界不够稳定。

### 3. 扩展层应始终从属于主链路

任何更重的探索式场景层，都只能建立在当前主线已经成立之后。

---

## 当前处理口径

对于当前项目：

- 不把 Godot 作为近期主线工程
- 不为 Godot 回推新的主协议定义
- 不为了 Godot 重开旧地图 / 场景表现主线
- 只在需要讨论下游消费层时，把本文档作为历史参考

---

## 一句话结论

[docs/GODOT_INTEGRATION.md](docs/GODOT_INTEGRATION.md) 记录的是 FableMap 曾经的 Godot 消费层设想，而不是当前“真实底图上的地点、角色、事件、聊天与记忆体验”主线。
