# FableMap 玩家状态系统设计 v0.2

## 文档定位

这份文档定义 FableMap 当前主线下的玩家状态设计。

它不再以“浏览器自绘 2D 地图主舞台”为主要消费场景，而是服务以下主链路：

> **真实底图上的地点选择 -> 角色遭遇 / 地点事件 -> 聊天叙事 -> writeback / memory -> 回访反馈**

因此，玩家状态不应再主要围绕“地图移动表现”组织，而应围绕：

1. 玩家当前位于哪个真实地点语境
2. 玩家正与哪个地点 / 角色 / 事件发生关系
3. 世界会如何解释和回应玩家
4. 哪些痕迹正在沉淀为长期记忆
5. 玩家长期正在变成怎样的人

---

## 设计总纲

玩家状态变化，不应只是属性增长，而应是**玩家与城市互相塑形的过程**。

也就是说：

- 你靠近哪些地方，决定哪些地点开始回应你
- 你停留、观察、标记、对话过什么，决定哪些地点开始记住你
- 你偏爱什么角色与事件，决定世界如何编排后续遭遇
- 你长期留下怎样的痕迹，决定城市如何重新命名你

玩家状态系统因此应同时服务：

- 真实底图上的地点选择与邻近体验
- 地点详情面板与事件入口
- 角色遭遇与聊天主舞台
- 写回协议、治理协议与世界记忆系统
- 长期人格、审美倾向与回访体验

---

## 三层结构

玩家状态建议拆成三层：

1. **即时状态层**：分钟级或秒级变化，驱动当前地点体验与聊天上下文
2. **世界关系层**：中频变化，定义玩家与地点、区域、角色、阵营之间的关系
3. **长期人格 / 审美层**：低频变化，定义玩家最终会变成什么样的人

这样的拆分可以同时兼容：

- 当前地点语境
- 角色回应与事件开放条件
- 地点熟悉度与回访反馈
- 世界记忆沉淀
- 长期身份形成

---

## 一、即时状态层

即时状态层适合优先落地在当前地点面板、聊天场景与写回交互里。

特点：

- 高频变化
- 强上下文相关
- 直接影响地点、角色、事件与反馈语气

### 1. 位置状态

位置不应只是一组坐标，而应是“语义位置 + 当前地点关系”。

建议字段：

- `current_position`：当前坐标
- `current_zone_id`：当前所在区域
- `nearest_poi_id`：最近地点
- `active_place_id`：当前聚焦地点
- `place_context`：当前地点语境，如 `commercial / transit / residential / medical / academic / park`
- `entry_mode`：当前位置是通过 `map_select / nearby / revisit / event_jump` 哪种方式进入

用途：

- 决定地点面板当前展示的地点语义
- 决定角色 / 事件的默认入口与排序
- 决定聊天场景如何承接当前上下文
- 决定写回事件写入哪个地点锚点

### 2. 行动状态

行动状态不再主要围绕地图行走，而围绕地点体验状态机组织。

建议状态：

- `idle`：暂未进入明确体验
- `browsing_map`：在真实底图上浏览与选点
- `approaching_place`：正在接近目标地点
- `observing`：观察地点中
- `interacting`：与地点对象交互中
- `in_event`：正在经历地点事件
- `in_dialogue`：正在与角色或地点意识对话
- `writing_back`：正在提交写回
- `revisiting`：回访旧地点 / 旧记忆中

用途：

- 决定 UI 当前应突出地点、角色、事件还是对话
- 决定编排器下一步最该返回什么
- 决定同一地点是否应优先展示旧记忆、即时事件或聊天入口

### 3. 感知状态

感知状态不是失败惩罚，而是世界解释器与叙事滤镜控制器。

建议最小化为 3 到 5 个轻量指标：

- `clarity`：清明度，影响地点解释是否清晰、有序
- `tension`：紧张度，影响事件压力、异常提示与语气不安度
- `attunement`：感应度，影响隐藏角色、回声、记忆裂缝是否可见
- `fatigue`：疲惫度，影响信息密度、事件推进节奏与回访建议
- `warmth`：情绪温度，影响治愈型地点、温柔型角色与私密叙事的开启概率

建议语义：

- `clarity` 高：地点解释更稳定、命名更明确、反馈更理性
- `tension` 高：异常事件更容易前置、角色语气更警觉
- `attunement` 高：隐藏记忆、人物暗示、地点细节更易浮现
- `fatigue` 高：系统更倾向推荐收束、休整或延迟复杂事件
- `warmth` 高：更容易触发安抚性回应、回访邀请与情绪共鸣

### 4. 当前关注状态

为了适应“地点 / 角色 / 事件 / 聊天”主线，建议显式区分当前关注对象：

- `focus_target_type`：`place / character / event / memory / route`
- `focus_target_id`
- `active_event_id`
- `active_character_id`
- `active_dialogue_id`

用途：

- 决定聊天主舞台当前对应谁
- 决定 writeback 返回该挂接到哪个对象
- 决定 orchestrator 如何排序下一步建议

---

## 二、世界关系层

这一层不是“我是谁”，而是“这片地方、这些角色与这座城市怎么看我”。

变化频率中等，适合在一次出行、一段探索或一段连续剧情中渐进变化。

### 1. 地点熟悉度

地点熟悉度是当前主线的核心状态之一。

建议字段：

- `zone_familiarity.<zone_id>`
- `poi_familiarity.<poi_id>`
- `route_familiarity.<route_id>`
- `place_visit_count.<poi_id>`
- `last_visit_at.<poi_id>`

用途：

- 决定地点是否从陌生入口升级为可回访关系点
- 决定地点详情展示深度与角色开放度
- 决定回访时是否优先出现旧记忆与未完成事件
- 形成“这个地点开始记住你了”的长期反馈

### 2. 角色关系

在当前主线下，角色关系应成为一等公民。

建议字段：

- `character_bond.<character_id>.familiarity`
- `character_bond.<character_id>.trust`
- `character_bond.<character_id>.heat`
- `character_bond.<character_id>.last_interaction_at`
- `character_bond.<character_id>.open_threads`

用途：

- 决定角色是否主动出现
- 决定角色语气与可触发事件强度
- 决定聊天中可见的话题、秘密与回访钩子

### 3. 阵营关系

阵营状态不建议只做全局好感，而建议按阵营拆多个维度。

建议字段结构：

- `faction_standing.<faction_id>.favor`
- `faction_standing.<faction_id>.trust`
- `faction_standing.<faction_id>.heat`
- `faction_standing.<faction_id>.debt`

用途：

- 决定某些地点事件是否开放
- 决定广播、传闻、角色立场与公共回应
- 决定城市如何命名与识别玩家

### 4. 地点痕迹 / 回声

每个地点或区域都应能记录玩家留下的行为印记。

建议字段：

- `left_messages_count`
- `marks_created`
- `events_resolved`
- `events_ignored`
- `memories_bound`
- `public_actions_taken`
- `recent_echo_links`

用途：

- 改变地点与角色对玩家的回应方式
- 让某个地点成为个人锚点
- 让未来聊天直接引用既有行为
- 为写回与世界记忆系统提供依据

---

## 三、长期人格 / 审美层

这一层是长期存档与身份形成的核心。

不是升级，而是玩家逐渐长成什么样的人。

### 1. 漫游者画像

不建议定义固定职业，而建议跟踪长期行为偏好，生成动态 archetype。

示例倾向：

- 偏探索：`wanderer` / `boundary_scout`
- 偏社交：`mediator` / `messenger`
- 偏黑暗线：`rift_archivist` / `night_patroller`
- 偏治愈线：`echo_gatherer` / `greenhouse_keeper`
- 偏记忆线：`memory_keeper` / `quiet_returner`

### 2. 审美与叙事亲和

地点体验不是纯规则系统，长期还应记录玩家偏好的叙事类型与审美倾向。

建议字段：

- `style_affinity.dark_satire`
- `style_affinity.healing`
- `style_affinity.mundane_intimacy`
- `style_affinity.mythic_reality`
- `narrative_affinity.confession`
- `narrative_affinity.mystery`
- `narrative_affinity.reunion`
- `narrative_affinity.anomaly`

用途：

- 决定同一地点更偏向触发哪种讲述方式
- 决定角色更可能用什么语气接近玩家
- 决定哪些记忆更容易被唤醒

### 3. 回访倾向与长期锚点

建议增加长期地点关系字段：

- `anchor_places[]`
- `home_style`
- `return_tendency.<poi_id>`
- `memory_weight.<poi_id>`

用途：

- 支撑回访推荐
- 支撑私密地点成长
- 支撑“这不是第一次来”的叙事连续性

---

## 四、推荐数据结构

```json
{
  "player_id": "player_local",
  "current_position": { "lat": 35.658, "lng": 139.701 },
  "current_zone_id": "zone_shibuya_core",
  "nearest_poi_id": "poi_clocktower_01",
  "active_place_id": "poi_clocktower_01",
  "entry_mode": "map_select",
  "action_state": "in_dialogue",
  "focus_target_type": "character",
  "focus_target_id": "char_clock_keeper",
  "active_event_id": "evt_night_bell_01",
  "active_character_id": "char_clock_keeper",
  "active_dialogue_id": "dlg_clock_20260409_01",
  "clarity": 72,
  "tension": 28,
  "attunement": 41,
  "fatigue": 19,
  "warmth": 57,
  "zone_familiarity": {
    "zone_shibuya_core": 3
  },
  "poi_familiarity": {
    "poi_clocktower_01": 5
  },
  "place_visit_count": {
    "poi_clocktower_01": 4
  },
  "character_bond": {
    "char_clock_keeper": {
      "familiarity": 4,
      "trust": 2,
      "heat": 1,
      "last_interaction_at": "2026-04-09T07:30:00Z",
      "open_threads": ["night_bell", "late_return"]
    }
  },
  "faction_standing": {
    "trade_guild": {
      "favor": 12,
      "trust": 8,
      "heat": 1,
      "debt": 0
    }
  },
  "anchor_places": ["poi_clocktower_01"],
  "home_style": "warm_corner",
  "style_affinity": {
    "dark_satire": 0.2,
    "healing": 0.8,
    "mundane_intimacy": 0.7,
    "mythic_reality": 0.6
  },
  "narrative_affinity": {
    "confession": 0.7,
    "mystery": 0.3,
    "reunion": 0.8,
    "anomaly": 0.2
  }
}
```

---

## 五、展示建议

### 建议展示结构

- 玩家状态
- 当前地点：`Clocktower Crossing`
- 当前行动：`对话中`
- 当前聚焦：`守钟人`
- 当前事件：`夜钟回响`
- 感知：`清明 72 / 紧张 28 / 感应 41`
- 地点熟悉：`旧钟楼 Lv.5`
- 关系回响：`守钟人开始认得你`
- 长期锚点：`Clocktower Crossing`

### 展示原则

- 顶部显示即时地点与当前行动
- 中部显示当前角色 / 事件关系与感知
- 底部显示长期连接项，如锚点地点、家园风格或画像标签
- 避免堆叠过多纯数字，优先展示“世界内解释”

---

## 六、与现有系统的接口关系

### 1. 与真实底图入口的关系

玩家状态为真实底图层提供：

- 当前坐标
- 当前聚焦地点
- 附近地点排序依据
- 回访优先级与当前位置语义

### 2. 与地点 / 事件系统的关系

玩家状态为地点与事件系统提供：

- 当前地点语境
- 事件开放条件
- 熟悉度与记忆权重
- 当前是否处于观察、事件或回访态

### 3. 与角色 / 聊天系统的关系

玩家状态为角色与聊天系统提供：

- 当前关注对象
- 角色关系强度
- 感知指标
- 当前叙事倾向
- 历史互动痕迹

### 4. 与写回系统的关系

玩家状态为写回系统提供：

- `action_state`
- 当前地点与目标对象
- 感知波动
- 熟悉度与关系变化挂接点
- 私密痕迹与公共痕迹沉淀基础

### 5. 与动态扰动 / 编排系统的关系

动态扰动与编排系统反向影响：

- `clarity`
- `tension`
- 当前事件优先级
- 角色出现概率
- 回访钩子与广播建议

---

## 七、实现建议顺序

### 第一阶段

优先落地：

- `action_state`
- `active_place_id`
- `focus_target_type / focus_target_id`
- `clarity / tension / attunement`
- `zone_familiarity / poi_familiarity`
- `character_bond`
- `home_style`

### 第二阶段

继续扩展：

- `active_event_id / active_character_id / active_dialogue_id`
- `place_visit_count`
- `anchor_places`
- `public_actions_taken`
- `narrative_affinity`

### 第三阶段

进入长期系统：

- 回访倾向建模
- 个性化角色回应
- 城市长期记忆引用
- 同一地点的连续关系史

---

## 八、结论

FableMap 的玩家状态系统，核心不应是“地图里怎么移动”，而应是：

**玩家如何进入一个真实地点、被角色与事件回应、留下可持续记忆，并在回访中与城市形成关系。**

因此，玩家状态变化应被视为：

- 地点关系的变化
- 角色关系的变化
- 感知与行动方式的变化
- 长期人格与审美取向的形成
- 玩家在城市中留下的痕迹不断沉积

这也是玩家从游客转变为共创者的关键接口。
