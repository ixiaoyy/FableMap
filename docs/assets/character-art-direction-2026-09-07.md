# 正式角色美术基准（2026-09-07）

用户在拒绝两轮源码像素角色后，明确认可本轮 ImageGen 生成的男女四向角色底稿，原话为“这种可以”。这张图取代前两版程序绘制角色作为后续美术目标，不能继续将已被拒绝的 v2 描述为审美通过。

## 采用的视觉特征

- 紧凑而连贯的头、肩颈、躯干、腰胯与腿部轮廓。
- 蓬松、成簇的栗色头发；温和清楚的眼睛和圆润下颌。
- 暖白衬衫、蓝色背带装、黄铜扣与小棕色工作靴的材质层次。
- 男、女四个朝向的主体比例和服装细节保持一致。
- 保留 A「清新田园」方向；不将高分辨率设计图直接等同于可用游戏图集。

## 制作来源

- 制作者：镜像岛项目通过 OpenAI 内置 ImageGen 辅助制作。
- 本轮直接输入为已选 A 方向示例及星露谷官方农场截图；后者只研究人物比例，不作为项目运行时素材采用。
- 原始输出：`C:/Users/phpxi/.codex/generated_images/01a0725c-3d0e-7640-b4cb-c438941e0e4f/exec-ad6cbafb-8092-4484-be89-e00068284044.png`。
- 工作区底稿、完整真实 prompt、文件尺寸与哈希记录在被忽略的 `artifacts/character-production-2026-09-07/`。

## 实施边界

正式功能继续采用独立头部、上装、下装和性别选择，正常 `/` 使用同一套素材，不回到整人预设。当前 GameSession 的外观字段与 IndexedDB v13 继续保留。

这张底稿本身仍是静止四向设计；后续已据此制作六组动作源图，整理成 v3 角色部件与材质遮罩两张 432×1536 PNG，并按 CDN 不可变对象和精确 manifest 发布，媒体配置与来源文本进入 main `633f29e`。本地正常 `/` 已接入共享的 Vue/Phaser 分层角色：48×64 单帧、144×256 部件图集、脚底 `(24,60)`、世界 scale 0.5，源码负责裁切与调色。完整来源、实际文件与处理边界见 [v3 素材记录](islander-raster-character-v3-2026-09-07.md)。

本地类型与客户端构建通过，已查看正常入口的新角色，不等于所有动画获认可。侧面目前仍为接触/站立循环，完整交替肢体动作需继续精修并发布新不可变版本；已被拒绝的源码角色 v2 不作为回退方案或通过记录。禁止用 base64、像素数字编码或本地专用预览参数绕过正式素材发布。
