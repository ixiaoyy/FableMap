# 2026-04-21 成熟框架化重构优先级调整

## 背景

项目内容已经从单一演示页扩展到地图发现、模板、店主后台、酒馆房间、单聊、群聊、记忆、世界书、玩法和 LLM 配置等多条链路。继续堆功能会放大大组件、大 router、大 service 的维护成本。

## 改动

- 将 `FM-VT-REF-00` 成熟框架化重构总计划写入当前最高优先级。
- 将 `FM-VT-REF-01~04` 拆为前端路由模块化、前端数据流收敛、后端 FastAPI 分层、API 契约验证四个后续切片。
- 框架化重构的后续实现边界以 Trellis 任务留痕为准。
- 在架构总入口补充当前 P0 架构决定。

## 明确不做

- 本次不安装新依赖、不修改运行代码、不改变 Schema 或 API 行为。
- 不借重构引入大型 UI 框架、状态管理库、数据库、地图渲染依赖。
- 不改变主人主权、Token 自主、真实地图锚点、访客隐私和 SillyTavern 兼容边界。

## 验证

- 文档链接和任务 ID 使用 grep 检查。
- 文档改动不需要运行前后端构建。
