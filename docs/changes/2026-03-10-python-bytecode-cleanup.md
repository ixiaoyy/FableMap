# 2026-03-10 Python 字节码文件清理说明

## 本次做了什么

- 从仓库中移除已跟踪的 Python `__pycache__` / `.pyc` 文件
- 让现有 `.gitignore` 规则真正生效，避免后续继续污染工作区

## 为什么要做

这些文件是本地运行产物，不属于源码或测试资产。若继续保留，会导致无意义 diff 和提交噪音。

## 影响范围

- 仓库清洁度
- 后续 Python 开发体验

## 明确没有改什么

- 没有修改 CLI 接口
- 没有修改 Overpass 请求逻辑
- 没有修改世界构建逻辑
- 没有修改测试逻辑

## 是否涉及协议 / Schema / 命名变更

- 不涉及协议变更
- 不涉及 Schema 变更
- 不涉及运行时代码接口变更

## 验证方式

- 检查 `git ls-files` 不再包含 `__pycache__` 或 `.pyc`
- 检查 `git status --short --branch` 工作区干净

## 风险点

- 仅为仓库清理，无运行时风险