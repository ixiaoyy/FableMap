# 模块认领说明

- 模块名 / 区域名：附近地图真正页面入口第一版
- 负责人：Augment Agent
- 改动类型：功能
- 当前状态：complete

## 目标

把“附近真实地图 → 异世界预览”从命令触发进一步推进到页面驱动：新增一个标准库本地页面服务，让用户通过页面输入坐标并直接看到生成后的预览页。

## 本次重点

- 新增本地页面服务模块，提供页面入口与 `nearby` 生成 API
- 把仓库根目录 `index.html` 改成可交互页面壳
- 尽量复用现有 `nearby` / `bundle` 逻辑，避免重写生成链路

## 计划修改范围

- 新增页面服务模块与相关测试
- 适度整理 `nearby` 逻辑，提取可复用 helper
- 更新 CLI 以接入页面入口
- 更新根目录 `index.html`、`README.md` 与本次变更说明

## 明确不改范围

- 不修改 `WORLD_SCHEMA`
- 不引入第三方依赖或完整 Web 框架
- 不重写现有 `world_builder` 幻想映射规则
- 不在本次实现浏览器端动态底图或复杂选点 UI

## 验证方式

- 新增页面服务相关单测
- 回归 `nearby / cli / bundle / demo / showcase` 测试
- 进行本地页面服务 smoke，确认页面 API 与预览输出可用
- 执行 `git diff --check`

## 风险与备注

- 浏览器不能直接从静态文件调用本地 Python，因此仍需一个本地服务进程承接页面请求
- 第一版目标是“真正页面入口”，不是完整地图 Web App