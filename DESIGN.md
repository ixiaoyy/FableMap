# Mirror Island Design

## Visual Thesis

镜像岛的界面是一幅可以进入的东方田园画卷：温润但不怀旧仿古，明亮但不过度梦幻。启动页负责建立归园仪式；进入游戏后，界面退回像素生活世界的实用叙事。

## Palette

- `mist-sky`：带青的浅天色，承载大面积空气与远山。
- `paper`：暖白宣纸，用于启动面板和高信息密度表面。
- `bamboo`：深竹青，负责品牌字、主按钮和可用状态。
- `harvest`：低饱和稻穗金，用于描边、纹样和重点装饰。
- `seal`：克制朱砂红，只用于印章式品牌点和错误强调。
- `ink`：墨褐而非纯黑，作为正文与木构阴影。

## Materials and Components

- 纸张使用轻微纤维与水印式云纹，避免通用白色卡片。
- 面板边界来自木构、细金线和卷轴结构；按钮像漆木牌，但保持标准 HTML 控件语义。
- 状态提示采用小型悬牌，不使用技术仪表盘、霓虹灯或玻璃拟态。
- 装饰只取自田园世界：竹叶、稻穗、云气、印章、木材与水墨远山。

## Typography

- 中文品牌与大标题优先使用可用的宋楷/书卷型本地字体栈，正文使用清晰的人文宋体栈。
- 英文 `MIRROR ISLAND` 只作为低层级品牌注记，使用宽字距罗马体。
- 关键操作文字保持直接，不使用难读的纯书法字形。

## Composition

- 桌面启动页以全幅主视觉为场，左侧 34%–42% 为竖向画卷面板，右侧保留农舍、菜畦与入岛路径作为空间邀请。
- 存档状态位于右上视觉远端，底部信息带像画纸自然收口。
- 移动端将画卷面板收为可滚动单列，背景退为氛围，所有操作先于装饰。

## Motion

- 只保留一次页面展开：背景由轻雾显现，画卷以短距离下落与阴影稳定进入。
- hover 通过漆色、金线和轻微位移表达；不使用发光、弹跳或持续漂浮。
- `prefers-reduced-motion` 下取消进入动画和非必要过渡。

## Accessibility

- 正文与按钮满足 WCAG AA 对比度，focus-visible 使用纸白与墨绿双层轮廓。
- 文字、状态和操作都由 DOM 渲染，不写进背景图。
- 禁用、加载、错误和无存档状态必须同时依赖文字与形状，不只依赖颜色。

## Prohibitions

- 不回到深色技术控制台、酸绿色网格或通用 SaaS 卡片语言。
- 不把文字、按钮、存档状态或品牌标识烘焙进图片。
- 不使用玻璃拟态、霓虹光晕、紫色渐变、通用圆角卡片或无语义装饰图标。
- 不让启动页视觉规则外溢并重排已经冻结的 Phaser 地图构图。
