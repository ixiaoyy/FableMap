# 技能前置批次实施清单

## S2-B 稻草人奖励闭环

本批已完成代码接入、定向验证、隔离浏览器检查与两端导出；实际范围和未验证项见 [S2-B 记录](verification-s2b.md)。

1. 核对配方、煤炭来源与乌鸦边界，建立 knownRecipes / 报告合同。
2. 接入煤炭购买、解锁确认、制作和一格世界物件，保留旧箱子与出货链。
3. 隔夜生长后判定乌鸦、保护计数与损失报告，严格验证存档。
4. 接入图标 / 场景和正常输入，检查本地占位，记录正式美术仍待验收。
5. 定向验证原子性与边界，必要解析 / 导出通过后暂存生产文件。

S1-D 与 S2-A 已完成当前代码接入和定向检查，实际证据与仍未实现部分见 [验证记录](verification-s2a.md)。用户已确认在既定开发方向内连续推进，不对每个小步骤重复发起批准；原作未知关键事实、改变已确认业务规则和远程副作用仍按项目边界处理。

## S2-A 首批经验与熟练度

1. 将已核实的三技能、六种作物经验写入真实状态 / 内容，逐个成功入口接入。
2. 工具耗能与水壶预算复用单格成本；日结持久报告记录升级并处理满体力恢复。
3. 完成当前存档严格校验、只读技能页与升级报告显示；配方职业不做假按钮。
4. 定向验证经验时点、土豆按株计、混合镰刀、阈值与小数成本、失败重试、日结恢复和坏档拒绝。
5. 最终检查和两端导出，暂存生产代码，保留真人验收 / 后续完整技能待办。

## S1-D 土豆基础产出

1. 核实基础概率与现有调用，保留每日运气缺口。
2. 同步土豆 ID / 数据 / 媒体语义键与 codec，使用有界的几何分布计算，删除旧产出分支。
3. 定向验证产量分布、稳定结果、联合容量及保存重试，不扩建测试矩阵。
4. 最终解析 / 类型检查、必要导出通过后暂存生产文件，真人验收与原作运气校准分开记录。

## S1-B 执行中

三种作物已接入并通过最小检查。羽衣甘蓝采用用户进一步批准的临时规则，T-SCYTHE-01 / 02 保留待校准，真人验收待反馈。详细进度见 [阶段记录](verification-s1b.md)。

1. 按 [S1-B 方案](research/s1b-interactions.md) 核实普通镰刀命中与架子重叠 / 清除边界；证据不足不猜测。
2. 确认三种内容替换与交互范围后，枚举所有调用方、当前版本和已有暂存改动。
3. 依次接入内容属性与 ID、领域收获、世界碰撞、正常输入与提示、严格存档版本；不跨入土豆、地表资源或技能奖励。
4. 完成最小定向检查、两端导出，留下真人验收记录；生产代码暂存，S1-A 改动保留。

## 2026-09-09 规划交付

- [x] 用户批准继续现有任务的研究和规划，不视为下一批实现授权。
- [x] 逐项核对现有六种作物、六种鱼、采集和地表石头入口。
- [x] 记录网页来源、版本限制、映射建议及不等价分支。
- [x] 更新 PRD、下一批设计、执行顺序和最小验证清单。
- [x] 用户审阅并批准 S1-A 的具体代码与开发存档影响。

## S1-A 执行记录

以下步骤的代码修改、最小检查与 Web / Windows 导出已完成；生产内容与 codec 已暂存，测试和文档未暂存。真人步骤未执行，任务不归档。详见 [验证记录](verification-s1a.md)。

1. 确认批准范围仅防风草与花椰菜基础内容；使用当前任务，不将整个 `in_progress` 状态当作后续批次授权。
2. 读取 `trellis-before-dev` 和当前 Godot 规则，检查完整 staged / unstaged diff；若已有别人新增版本，重新确定开发档版本号。
3. 枚举 `turnip`、`turnip-seed`、萝卜以及两种作物的全部当前内容引用；核对 `rules.json`、`dialogues.json`、`media.json` 的实际消费。生成目录不手改。
4. 同步 ID、名称、生长天数、种价、`prices` 售价、食用体力；只维护有实际调用方的数据，不先添加未消费 XP 字段。
5. 同步封套 / 状态版本和初始状态，不写玩家槽，不增加迁移或 alias。对既有窄检查中的相关期望做定向调整。
6. 最终改动后运行一次 `npm --prefix ./apps/mirror-island run typecheck:client`。需要可玩产物时再执行 `build:client` / `build:windows`，不运行服务端、Prisma 或全量旧测试。
7. 复核内容 JSON 可解析、所有新 ID 引用可解析、旧运行 ID 无残留、Git diff 格式正常；若改媒体映射，确认 URL / key / 尺寸 / MIME / 字节 / SHA-256 与登记项一致，Git 跟踪图片二进制仍为零。未查询的远端缓存头标记未验证，不假称线上核验。
8. 按下表留下真人验收步骤；默认不打开可见窗口。除非用户授权，不自行操作玩家档。
9. 最小检查通过后暂存本批生产代码 / 配置；文档和测试不自动暂存，不 commit / push / 部署。

| 层次 | 核验与预期 | 证据限制 |
|---|---|---|
| 静态内容 | 新 ID、商店售价、食用恢复、两种作物天数一致，旧 ID 不残留于当前运行数据 | 不证明实际玩法 |
| 真人种植 | 隔离新档，分别播种并按天浇水，4 / 12 次有效生长后成熟；漏浇一天不得增长 | 不加速真实玩家档或代签真人通过 |
| 真人经济 | 购买、出售、出货分别使用 20/80 与 35/175；食用恢复 25/75，上限 270 | 日结成功与保存失败重试分别记录 |
| 存档 | 同版本继续保留地块；旧版本明确拒绝且原文件 / 记录未被改写 | Web 与 Windows 分别报告 |
| 视觉 | 背包、详情、农田、商店和相关文本名称一致；原素材保持临时状态 | 名称正确不等于美术质量通过 |

后续批次不自动启动：S1-B 解决其余作物 / 地表资源；S1-C 解决钓鱼；S2 接入经验 / 等级 / 熟练度和首批真实夜间奖励；S3 随各系统闭合配方 / 职业。具体依赖见研究文件。

此前规划轮仅检查文档；本次实施轮实际运行的检查与未验证项以验证记录为准。

## S0 历史实施结果

- [x] 检查当前 Godot 规则、内容表、存档校验与界面消费路径。
- [x] 核对经验时点与体力精度依赖，记录关键原作差异。
- [x] 更新旧的原创职业要求，保留完整复刻目标。
- [x] 确认 S0 的具体代码和开发存档版本范围。
- [x] 核对本批可达分支及水壶 Lv2 的中间状态，不凭旧占位值推导原作机制。
- [x] 激活当前任务，实施统一体力规则及相关调用方。
- [x] 同步初始状态、当前版本校验、HUD 与日结显示。
- [x] 在隔离内存/临时档中验证数值和失败重试；不触碰玩家槽。
- [x] Godot 检查与相关导出通过后暂存生产代码。
- [x] 回填实际验证和仍待对齐项，不把 S0 标成整个技能系统完成。

结果见 [S0 验证记录](verification-s0.md)。完整技能任务继续保持 in_progress，真人玩法验收待反馈。
