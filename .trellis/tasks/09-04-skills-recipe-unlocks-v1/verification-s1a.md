# S1-A 两种作物验证记录

日期：2026-09-09。基线 `196ba6df`，本轮变更尚未提交或发布。用户已明确批准 S1-A；完整技能任务继续保持 `in_progress`。

## 实际行为变化

- 当前萝卜 / 种子改为 `parsnip` / `parsnip-seed`，名称为防风草 / 防风草种子。委托、送礼和素材映射同步，不增加旧 ID alias。
- 防风草生长 4 天，种价 20、售价 35、食用恢复 25；花椰菜生长 12 天，种价 80、售价 175、食用恢复 75。
- Godot 封套 3、状态 15，旧封套拒绝且保留原记录。没有数据库迁移或存档迁移。
- 其他作物、开局金币、初始库存数量、既有 NPC 偏好数值、工具耗能和候选保存链不变。XP、技能和职业未接入。

## 已执行检查

| 检查 | 结果与实际覆盖 |
|---|---|
| 内容结构比较 | 与基线作结构化比较，只有本批明确的 ID / 文本 / 数值变化；所有 `itemId/cropId/seedId` 引用有效 |
| 素材映射比较 | 只改变 8 个语义键，URL / 帧 / 原素材名称不变；保留 `vectoraith-turnip-*` 表示原来源，不能伪称已制作防风草美术 |
| `typecheck:client` | 通过：地图准备 TypeScript、Godot 导入及 120 帧无窗口启动；未见脚本错误 |
| `test:energy` | `failures=[]`；实际内存会话验证新版本、旧版本拒绝不写入、小数食用恢复、上限与失败重试；隔离文件往返不使用玩家槽 |
| `test:godot` | 34 项规则案例与 5 个哈希通过；隔离真实文件验证新建 / 日结重试及继续读取 |
| Web / Windows 导出 | `build:client`、`build:windows` 均退出 0；执行器会检查 SCRIPT ERROR / ERROR，而非只依赖进程退出码 |
| 媒体准备 | 两次导出的准备步骤均确认 40 个登记源素材；Git 跟踪图片二进制为零 |

旧 fixture 仅精确替换萝卜 ID、收获案例名称，以及防风草第 3 次生长尚未成熟 / 成熟收获起点为 4 天这两项期望；保留原单行格式，没有依据新程序批量重生成期望。结构化比较确认无其他变化。S0 检查中的合法会话起点显式采用当前版本，旧档拒绝样本改为上一版 2 / 14。

产物位于 `apps/mirror-island/godot/exports/web/` 与 `exports/windows/`。本次 Web / Windows PCK 均为 29,076,296 字节；Windows exe 为 109,268,480 字节。Node strip-types 实验性提示仍存在，不影响导出成功。

## 未验证与临时差异

- 未进行本批真人连续种植、实际购买 / 食用 / 出货全流程、浏览器刷新恢复或 Windows 图形界面验收。静态数值与既有规则检查不能代替 4 / 12 天实际玩法结果。
- 未打开可见游戏窗口，未操作玩家存档；未查询数据库、上传素材、核验线上缓存头或部署。
- 防风草仍使用已登记萝卜图集帧作为临时外观。种植阶段、品质、季节枯萎、巨型作物和剩余原作内容均未宣称完成。
- 核验的源素材为既有不可变登记项；本轮没有变更 URL、PNG 内容或 CDN 配置，不声称重新确认线上响应。

## 回退

按本批 diff 同时恢复内容定义、相关引用和 codec 版本，不整文件覆盖混合改动，不回退 S0。回到 2 / 14 后应明确拒绝本批 3 / 15 档；不转换或重写任一玩家记录。

生产内容 / codec 已暂存；测试、任务记录和说明文档未自动暂存。没有 commit、push 或部署。
