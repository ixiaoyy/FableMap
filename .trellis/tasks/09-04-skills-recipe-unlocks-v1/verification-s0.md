# S0 体力基础规则验证

日期：2026-09-08。用户已确认本批范围。S0 已实现，完整技能任务保持 in_progress；自动检查不代替真人玩法验收。

## 行为变化

- `FarmEnergyRules` 统一体力上限 270，锄头、斧头、镐子与基础浇水每次消耗 2，抛竿消耗 8。
- 状态、食物恢复、睡眠恢复和存档保留小数，仅 HUD 与日结文字取整。
- 正常睡眠恢复到 270；晚睡按时间扣减恢复量，不把高于恢复目标的睡前体力降低。
- 存档封装升级为 v2，状态升级为 v14。旧封装或旧状态拒绝继续，不自动转换、覆盖或删除。实际存储容器名称保持不变；新版本使用新建开发农场，手动新建仍需经过现有确认入口。
- 水壶 Lv2 暂保留原三格与 20/40 容量，每个实际浇水格消耗 1 水和 2 体力，独立限制两种余量；这不是原作铜水壶完整机制。

## 实际验证

全部在后台运行，无前台游戏窗口。内存仓库和临时文件隔离于玩家存档槽。

- `npm --prefix ./apps/mirror-island run typecheck:client` 通过。
- `npm --prefix ./apps/mirror-island run test:energy`：`ENERGY S0 failures=[]`。覆盖小数编解码、非法体力拒绝、旧档拒绝且原记录不变、劳动保存失败及重试不重复扣除、浇水预算、重复抛竿、食物封顶、晚睡分段与高余量保留、睡眠保存失败及重试、HUD 数值。
- 隔离原子文件保存与读取通过，路径 `artifacts/energy-s0-2026-09-08/roundtrip.json`。
- `npm --prefix ./apps/mirror-island run test:godot`：`PARITY 34 cases; hash 5; failures=[]`。夹具仅更新状态版本，以及受本批影响的浇水、钓鱼期望值。
- `npm --prefix ./apps/mirror-island run build:tool-art` 通过，Web 与 Windows 本地导出均完成，保留本地新工具素材预览。构建日志未发现脚本错误或警告。
- 日志保存在 `artifacts/energy-s0-2026-09-08/`。生产代码与配置已暂存；测试、文档和诊断产物不自动暂存。

## 未完成范围与回退

熟练度、经验、职业、升级配方、疲劳、负体力、永久体力成长及完整金属工具机制未在 S0 实现。现有采集收益、日期推进和金币结算规则保持；此次没有数据库连接、上传、部署、提交或推送。

真人实际游玩与浏览器 IndexedDB 恢复仍待验收，本次内存和文件验证不代表浏览器持久化验收通过。回退本批须协调还原共享体力规则、各调用方、初始状态和编解码版本；保留原玩家文件，不能只降低版本号使不同规则的存档混用。
