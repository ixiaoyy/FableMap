# 实施清单

## 2026-09-08 旧引擎源码清理

用户确认继续完成迁移后，已完成当前仓库的源码与构建依赖收尾：

- 迁出 Tiled 解析和跨地图校验到 `scripts/content/`，移除旧运行期查询接口。室内绘图指令提升为 `godot/tools/interior-atlases.json`，由原生工具直接读取。
- 精确清理 144 个旧文件，包括旧客户端、TypeScript 玩法、Vue/Vite 客户端入口、旧快照生成和对照脚本、四个退役客户端测试。没有递归删除未跟踪文件。
- 移除 EasyStar 及仅由其引入的 heap，保留独立服务端的 TypeScript/Vite/tsx。
- 既有 34 个规则案例与 5 个哈希结果冻结到 `test/fixtures/godot-migration.json`，原有保存失败/重试检查继续执行，不依赖旧引擎或本机历史文件。
- `typecheck:client`（含地图准备类型、Godot 导入与 headless 启动）、服务端 `tsc`、`test:godot`、Web 与 Windows 导出通过；最终导出日志未发现 SCRIPT ERROR/ERROR。
- 清理前后 31 项生成地图、区域包装场景、内容数据与室内图像 SHA-256 全部一致。旧绘图生成副本已由准备脚本清除，工具源数据和测试 fixture 不随游戏导出。
- 生产代码/配置暂存；测试、说明和任务文档不自动暂存。没有 commit、push、媒体上传、部署或数据库连接；其他任务的未跟踪内容保留。

证据在 `artifacts/godot-retirement-2026-09-08/`。本轮完成源码依赖清理，不代签完整真人玩法、真机/长期存档、Linux 容器或线上发布验收，任务继续保持 in_progress。

回退必须按本轮 diff 精确恢复被删除的源码与构建引用，并同时还原 package.json/lockfile，不能只恢复旧文件或整仓库 restore；无数据库或存档迁移要回滚。以下是此前迁移阶段记录，旧源码保留与 EasyStar 说明已被本节取代。

- [x] 读取当前规范、检查干净工作区与迁移基线。
- [x] 明确用户授权为 Godot/GDScript 引擎迁移，真实城市方向暂不实施。
- [x] 获取并校验 Godot 4.7.2 标准版与 Web/Windows 导出模板。
- [x] 评估地图导入方案；采用 MIT YATI 2.2.7，固定 SHA-256 并记录两处释放补丁。
- [x] 建立可打开、可解析的 Godot 工程与素材准备脚本。
- [x] 导入 12 张地图、碰撞、出口、出生点与已有角色素材（地图与人物表现，非资源交互）。
- [x] 实现基础移动、区域切换和可编辑角色场景；完整工具/菜单输入随玩法移植。
- [x] 移植采集、种田、库存和制作。
- [x] 移植仓储、出货和建筑服务。
- [x] 移植时间、体力、天气、商店、钓鱼与日结。
- [x] 移植 NPC/宠物、对话、送礼、外观和目标。
- [x] 实现独立版本保存、严格解码、失败候选重试，并验证基础平台恢复。
- [ ] 完整真人玩法与真机/长期存档验收。
- [x] 生成 Web/Windows 产物并记录尺寸、启动与运行限制。
- [x] 完成必要解析、资源导入、导出、规则对照与场景接线检查；不代替真人验收。
- [x] 通过后暂存生产代码与配置；文档和测试不自动暂存。
- [x] 切换本地默认开发/构建，删除早期 Godot 预览入口并移除 Phaser/Vue 运行依赖。
- [ ] Linux 容器实测、字体媒体登记及正式发布；未获授权不执行上传或部署。

当前任务保持 in_progress，未完成项不得标记通过。默认不增加大规模测试矩阵；只保留必要的解析、导入、导出与针对性的运行检查。

## 完整代码接入后的证据

- `npm run typecheck`、`build:client`、`build:windows`、`build:server` 均通过；最终 Godot 日志没有脚本/运行错误。Node 22 的 strip-types 实验性提示仍存在。
- `test:godot`：34 项原 TS 状态案例、5 个哈希一致；包含半组、工具槽、作物、采集、仓储/推箱、再生和钓鱼。隔离真实文件检查新建失败重试、日结失败重试、重复重试、继续读取与坏库存拒绝。
- `tools/validate_views.gd` 使用内存仓库，32 项地图/菜单实例化与窄屏重排检查完成；不读写玩家槽。修复循环音频退出释放后日志干净。
- 实际 Web：新建；将锄头从第 1 格移至第 12 格，刷新继续后仍在第 12 格；锄地扣 2 体力；穿过农场河桥进入小镇；实际 02:00 被送回小屋，100g→90g、体力 50，进入次日日结报告。
- Docker context 是本机 Desktop pipe，但 daemon 未运行；没有启动可能自动带起其他数据库的 Docker Desktop。Linux 工具链与容器配置仅静态核查。
- 旧 TypeScript 图形/规则文件只作源内容与对照，生产游戏包不运行它们。EasyStar 0.4.4 仅保留为旧内容导出/对照的开发依赖。
- 尚未 commit、push、上传媒体或部署；后端、数据库、身份与旧浏览器开发存档未改动。

## 首批实际证据（2026-09-07）

- Godot 脚本、资源导入、120 帧 headless 启动通过；最后导入/导出日志未出现 ERROR 或 WARNING。日志在 artifacts/godot-runtime/ 下。
- Web 与 Windows release 导出通过；Windows 原生 exe headless 运行 120 帧后退出码为 0。没有代替 Windows 图形窗口真人验收。
- Web 实际查看原农场与分层角色，键盘移动并进入小屋；390×844 尺寸模拟发现并修复横屏留黑、高分屏缩放与中文缺字。不是实际手机触摸或首次蜂窝网络性能测试。
- 三处精修室内沿用旧 camera anchor；最终调整已经导出，完整 12 区真人路线仍待反馈。
- Web 未 HTTP 压缩：WASM 39,514,754 字节，PCK 16,295,260 字节，JS 279,815 字节；PCK 含完整中文字体，不把本地加载速度当作真实手机加载时间。
- 截图在 artifacts/godot-migration-2026-09-07/；字体为仅本地预览采用项，公开发布前仍须完成媒体上传登记。
- 生产代码/配置已暂存，任务/规范/说明文档未自动暂存；未 commit、push、部署或修改旧存档。
