# User cut references for light homepage

These files are copied from the user-provided `图标参考/` cuts for Trellis task `05-11-05-11-home-light-icon-reference-polish`.

Rules:
- Use these exact cuts as page materials: backgrounds, card covers, icons, and panel decorations.
- Keep text, navigation, CTAs, inputs, and links as real DOM.
- The original `图标参考/` files are left untouched; this directory is the runtime project asset copy.
