dist
lib
node_modules
pnpm-lock.yaml
.env.production
src/generated/prisma

# Phase 1 template images are local-only until provenance, hashes, and CDN publication are approved.
public/**/*.png
src/**/*.png

# Keep the legacy media preparation cache ignored; the Phaser mainline does not load this directory.
src/tiled/*
